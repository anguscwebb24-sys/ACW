# ACW under-construction page

This folder is ready to publish with GitHub Pages.

## Publish it

1. Create a new public GitHub repository.
2. Upload the **contents** of this folder to the repository root (not the containing folder).
3. Commit the files to the `main` branch.
4. Open **Settings → Pages** in the repository.
5. Under **Build and deployment**, choose **Deploy from a branch**, then select `main` and `/(root)`, and save.
6. Under **Custom domain**, enter `acwebb.com.au` and save.

The `CNAME` file is already set to `acwebb.com.au`. If that is not the intended domain, change both `CNAME` and the canonical/`og:url` values near the top of `index.html`.

## Contact details

- Phone: `0451 965 455` (link uses international format `+61451965455`)
- Email: `angus@acwebb.com.au`

## Files

- `index.html` — the complete page
- `CNAME` — tells GitHub Pages which custom domain to use
- `.nojekyll` — tells GitHub to serve the files exactly as uploaded
